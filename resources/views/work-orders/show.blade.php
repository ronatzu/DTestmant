<x-layouts.app title="Work Order Details"><div class="grid gap-6 lg:grid-cols-3"><div class="card p-6 lg:col-span-2"><div class="flex justify-between"><div><div class="font-semibold text-blue-700">{{ $order->number }}</div><h2 class="text-2xl font-bold">{{ $order->title }}</h2></div><a class="btn" href="{{ route('work-orders.edit',$order) }}">Edit</a></div><p class="mt-4 text-slate-600">{{ $order->description }}</p><div class="mt-6 grid gap-3 md:grid-cols-2"><div class="rounded-xl bg-slate-50 p-4">Asset<br><b>{{ $order->asset->name }}</b></div><div class="rounded-xl bg-slate-50 p-4">Technician<br><b>{{ $order->technician?->name }}</b></div></div></div><div class="card p-6"><h3 class="font-bold">Status Timeline</h3>@foreach($order->timeline as $event)<div class="mt-4 border-l-2 border-blue-200 pl-4"><x-badge :color="$event->status->color()">{{ $event->status->label() }}</x-badge><p class="mt-1 text-sm">{{ $event->note }}</p><p class="text-xs text-slate-500">{{ $event->changed_at->diffForHumans() }}</p></div>@endforeach</div></div></x-layouts.app>
