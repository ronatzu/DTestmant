<x-layouts.app title="Work Orders"><div class="mb-4 flex justify-between"><input class="input" placeholder="Search work orders"><a class="btn" href="{{ route('work-orders.create') }}">New Work Order</a></div><div class="grid gap-4 lg:grid-cols-2">@foreach($orders as $order)<a href="{{ route('work-orders.show',$order) }}" class="card p-5 hover:border-blue-300"><div class="flex justify-between"><b>{{ $order->number }}</b><x-badge :color="$order->priority->color()">{{ $order->priority->label() }}</x-badge></div><h3 class="mt-2 font-bold">{{ $order->title }}</h3><p class="text-sm text-slate-500">{{ $order->asset->name }} • {{ $order->technician?->name }}</p><div class="mt-3"><x-badge :color="$order->status->color()">{{ $order->status->label() }}</x-badge></div></a>@endforeach</div>{{ $orders->links() }}</x-layouts.app>
