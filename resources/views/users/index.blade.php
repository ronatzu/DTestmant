<x-layouts.app title="Users"><div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">@foreach($users as $user)<div class="card p-5"><div class="flex items-center gap-3"><div class="grid h-12 w-12 place-items-center rounded-full bg-blue-100 font-bold text-blue-700">{{ $user->avatar ?? str($user->name)->substr(0,2) }}</div><div><b>{{ $user->name }}</b><div class="text-sm text-slate-500">{{ $user->email }}</div></div></div><div class="mt-4 flex justify-between"><span>{{ $user->phone }}</span><x-badge>{{ $user->roles->first()?->name }}</x-badge></div></div>@endforeach</div></x-layouts.app>
