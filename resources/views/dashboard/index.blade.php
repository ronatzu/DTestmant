<x-layouts.app title="Dashboard"><div class="grid gap-4 md:grid-cols-5">@foreach(['Total Assets'=>$metrics['assets'],'Open Work Orders'=>$metrics['open'],'Completed Work Orders'=>$metrics['completed'],'Upcoming PM'=>$metrics['upcoming'],'Technicians'=>$metrics['technicians']] as $k=>$v)<div class="card p-5"><div class="text-sm text-slate-500">{{ $k }}</div><div class="mt-2 text-3xl font-extrabold">{{ $v }}</div></div>@endforeach</div><div class="mt-6 grid gap-6 lg:grid-cols-3"><div class="card p-6 lg:col-span-2"><h2 class="font-bold">Work Orders by Status</h2><div class="mt-5 space-y-3">@foreach($statusCounts as $s=>$n)<div><div class="mb-1 flex justify-between text-sm"><span>{{ str($s)->headline() }}</span><b>{{ $n }}</b></div><div class="h-3 rounded-full bg-slate-100"><div class="h-3 rounded-full bg-blue-600" style="width:{{ min(100,$n*4) }}%"></div></div></div>@endforeach</div></div><div class="card p-6"><h2 class="font-bold">Assets by Category</h2><div class="mt-4 space-y-3">@foreach($categoryCounts as $c=>$n)<div class="flex items-center justify-between rounded-xl bg-slate-50 p-3"><span>{{ $c }}</span><b>{{ $n }}</b></div>@endforeach</div></div></div><div class="mt-6 grid gap-6 lg:grid-cols-2"><div class="card p-6"><h2 class="font-bold">Maintenance Calendar</h2>@foreach($calendar as $pm)<div class="mt-3 flex items-center justify-between rounded-xl border p-3"><div><b>{{ $pm->asset->name }}</b><div class="text-sm text-slate-500">{{ $pm->frequency }}</div></div><span class="text-sm font-semibold text-blue-700">{{ $pm->next_maintenance->format('M d') }}</span></div>@endforeach</div><div class="card p-6"><h2 class="font-bold">Recent Activity</h2>@foreach($activity as $wo)<div class="mt-3 rounded-xl bg-slate-50 p-3"><b>{{ $wo->number }}</b> {{ $wo->title }}<div class="text-sm text-slate-500">{{ $wo->asset->name }} • {{ $wo->technician?->name }}</div></div>@endforeach</div></div></x-layouts.app>
